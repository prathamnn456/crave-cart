# Copy to ".env" for local dev, and set the same key in Vercel's dashboard for production.
# Public URL of the deployed BACKEND. No trailing slash.
# Local: http://localhost:4000   Prod example: https://your-backend.onrender.com
VITE_API_URL=http://localhost:4000
